name: new_weather
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  json_annotation: ^4.0.1 # Add json_annotation for serialization

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2 # Add build_runner to generate the code
  json_serializable: ^6.1.1 # Add json_serializable for JSON serialization

flutter:
  uses-material-design: true
